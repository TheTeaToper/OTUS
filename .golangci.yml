run:
  build-tags:
    - bench
    - ""
  tests: true

linters:
  disable-all: true
  enable:
    - asciicheck
    - bodyclose
    - copyloopvar
    - depguard
    - dogsled
    - dupl
    - durationcheck
    - errorlint
    - exhaustive
    - funlen
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godot
    - goheader
    - goprintffuncname
    - gosec
    - govet
    - importas
    - ineffassign
    - lll
    - makezero
    - misspell
    - nestif
    - nilerr
    - noctx
    - nolintlint
    - prealloc
    - predeclared
    - revive
    - staticcheck
    - tagliatelle
    - thelper
    - unconvert
    - unparam
    - unused
    - whitespace
    - gci
    - gofmt
    - gofumpt

linters-settings:
  depguard:
    rules:
      Main:
        files:
          - $all
          - '!$test'
        allow:
          - $gostd
      Test:
        files:
          - $test
        allow:
          - $gostd
          - ://github.com
  funlen:
    lines: 150
    statements: 80

issues:
  exclude-use-default: false
  
  # Правильное расположение пресетов в современной схеме v2
  exclude-presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling

  exclude-dirs:
    - third_party$
    - builtin$
    - examples$

  exclude-rules:
    # Ваше правило для тестовых файлов (без ошибочных полей)
    - path: _test\.go
      linters:
        - dupl
        - errcheck
        - gocyclo
        - gosec
